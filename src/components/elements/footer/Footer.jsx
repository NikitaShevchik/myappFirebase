import React from 'react';
import styles from './Footer.module.scss'

const Footer = () => {
    return (
        <div className={styles.footer}>
            Its footer and information in footer
        </div>
    )
}

export default Footer;